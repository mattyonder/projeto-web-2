@model IEnumerable<ProjetoWeb2.Models.Habilidades>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HabilidadeTxDescricao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HabilidadeTxDescricao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HabilidadeNrId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HabilidadeNrId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HabilidadeNrId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
